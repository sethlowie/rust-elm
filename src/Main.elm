module Main exposing (main)

import Html exposing (Html, div, h1, text)
import Html.Attributes exposing (class)


main : Html msg
main =
    div [ class "w-full h-full text-xl ml-10 bg-violet-500 text-black" ]
        [ text "waffles!!!"
        ]
